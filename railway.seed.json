{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "exec python scripts/seed_synthetic_data.py --reset",
    "restartPolicyType": "NEVER"
  }
}
